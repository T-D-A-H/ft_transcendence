http.host: 0.0.0.0
xpack.monitoring.elasticsearch.hosts:
- http://elasticsearch:9200
xpack.monitoring.enabled: true

# JVM heap size is set via LS_JAVA_OPTS in the environment (see ELK.yaml)

# Pipeline tuning for efficiency
pipeline.workers: 1
pipeline.batch.size: 50
